actor Act <3>
    receiver init()
        write("init at Act")
        self << R(1, {10, 20})
    end

    receiver R(int a, int[2] b)
        write(a)
        write(b[0])
        write(b[1])
    end
end